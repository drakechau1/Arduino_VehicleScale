#pragma once

enum Mode_t {
  SCALE,
  SETTING
};

enum Setting_State_t {
  SETTING_MOTOBIKE,
  SETTING_CAR,
  SETTING_OVERLOAD
};

enum Scale_Display_t {
  SCALE_WELCOME,
  SCALE_MOTOBIKE,
  SCALE_CAR,
  SCALE_OVERLOAD
};